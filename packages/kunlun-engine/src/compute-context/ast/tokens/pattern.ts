import { TokenType } from '../types';

export const word = new TokenType('word');
export const operator = new TokenType('operator');
export const unaryOperator = new TokenType('unary-operator');

export const _function = new TokenType('function');

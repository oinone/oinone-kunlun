import { DEFAULT_PREFIX } from '../../../mount';

export const component = 'card';

export const cssVars = {
  'action-background': `var(--${DEFAULT_PREFIX}-background)`,
  'action-border-color': `var(--${DEFAULT_PREFIX}-border-color)`,
  'action-divider-color': `var(--${DEFAULT_PREFIX}-border-color)`,
  'action-text-color': `var(--${DEFAULT_PREFIX}-primary-color)`
};
